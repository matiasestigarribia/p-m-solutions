# Environments & secrets
.env
.env.*
!.env.example
.venv/
venv/
env/
ENV/

# Local contact-sink data (SQLite) and uploaded media
data/
*.sqlite3
*.db

# One-shot local bootstrap scripts; never commit credentials tooling
scripts/create_admin.py
scripts/seed_cms.py

# Byte-compiled / packaging
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Tests & coverage
.pytest_cache/
.coverage
htmlcov/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# NOTE: _reference/ is intentionally NOT listed here — it is excluded locally
# via .git/info/exclude so the nested reusable-source repo is never committed.
